# ============================================
# DATABASE CONFIGURATION (NeonDB)
# ============================================

# NeonDB Connection URL - Get this from your Neon dashboard
# Use the POOLED connection string for serverless environments
# For Vercel: Add this in Vercel Dashboard → Project Settings → Environment Variables
DATABASE_URL="postgresql://username:password@ep-xxx-xxx-pooler.region.neon.tech/database?sslmode=require"

# ============================================
# AUTHENTICATION SECRETS
# ============================================

# JWT Secret - generate with: openssl rand -base64 64
# IMPORTANT: Must be at least 32 characters
# For Vercel: Add this as an environment variable
JWT_SECRET="your-super-secure-jwt-secret-key-minimum-32-characters-long"

# JWT Refresh Secret - generate with: openssl rand -base64 64
# IMPORTANT: Must be at least 32 characters
JWT_REFRESH_SECRET="another-super-secure-refresh-secret-key-minimum-32-chars"

# Token expiration times
ACCESS_TOKEN_EXPIRES_IN="15m"
REFRESH_TOKEN_EXPIRES_IN="7d"

# ============================================
# EXTERNAL SERVICES (ML Backends)
# ============================================

# ML Backend URL - For Vercel deployment, use your deployed ML API URL
# Options:
# 1. Deploy ML backend to Render (https://render.com) - Free tier available
# 2. Deploy to Railway (https://railway.app)
# 3. Use existing cloud deployment
NEXT_PUBLIC_ML_API_URL="http://localhost:8000"

# Vision Backend URL - For X-ray analysis
NEXT_PUBLIC_VISION_API_URL="http://localhost:8001"

# ============================================
# NEXT.JS SETTINGS
# ============================================

# Application URL - Vercel auto-sets this, but you can override
# For custom domain: https://curasense.yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# Environment
NODE_ENV="development"

# ============================================
# VERCEL DEPLOYMENT NOTES
# ============================================
# 
# Required Environment Variables for Vercel:
# 1. DATABASE_URL - Your Neon DB pooled connection string
# 2. JWT_SECRET - Generate a secure 64+ character string
# 3. JWT_REFRESH_SECRET - Another secure 64+ character string
# 4. NEXT_PUBLIC_ML_API_URL - URL of your deployed ML backend
# 5. NEXT_PUBLIC_VISION_API_URL - URL of your deployed Vision backend
#
# Optional:
# - ACCESS_TOKEN_EXPIRES_IN (default: 15m)
# - REFRESH_TOKEN_EXPIRES_IN (default: 7d)
#
# Note: The ML backends need to be hosted separately (Render, Railway, etc.)
# as they require Python/PyTorch which isn't supported in Vercel serverless.
